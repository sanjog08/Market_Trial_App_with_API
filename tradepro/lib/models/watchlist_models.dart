// here we write our stocks list class

class watchlist_class {
  String? id;
  String? symbol;
  String? company_name;
  num? rate;

  watchlist_class(this.id, this.symbol, this.company_name, this.rate);
}

class watchlist_temp {
  String name;

  watchlist_temp(this.name);
}